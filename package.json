{
  "name": "my-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "firebase:config": "node scripts/generate-firebase-config.mjs",
    "build": "npm run firebase:config && node scripts/build-site.mjs && tsc -p tsconfig.json",
    "copy:site": "node scripts/build-site.mjs && tsc -p tsconfig.json",
    "dev": "npm run firebase:config && npm run copy:site && tsc -p tsconfig.json && concurrently \"npm:watch\" \"npm:watch:site\" \"npm:serve\"",
    "watch": "tsc -p tsconfig.json --watch",
    "watch:site": "node scripts/watch-site.mjs",
    "serve": "node scripts/dev-server.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2",
    "typescript": "^5.9.3"
  }
}
