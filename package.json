{
  "name": "my-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "firebase:config": "node scripts/generate-firebase-config.mjs",
    "build": "npm run firebase:config && tsc -p tsconfig.json",
    "dev": "npm run firebase:config && concurrently \"npm:watch\" \"npm:serve\"",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "live-server Public --port=3000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.0",
    "typescript": "^5.9.3"
  }
}
